name: CI

on: [push]

jobs:
  deploy:
    if: github.ref == 'refs/heads/master'
    runs-on: [debian-latest]
    steps:
      - uses: actions/checkout@v1
      - name: Push to server
        uses: appleboy/ssh-action@master
        with:
          host: '201.46.121.218'
          username: 'admin'
          password: 'Wmbarros@2024!'
          script: cd /var/www/html/Teste && git pull